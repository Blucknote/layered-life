# Отчёт о проверках

Дата: 2026-09-05. Среда проверок: macOS 25.5.0 (arm64), Node v26.7.0,
npm 10.7.0; браузер — headless **Google Chrome for Testing 150** (puppeteer,
программный WebGL **SwiftShader**), окно 1600×1000. Итоговый скриншот —
`docs/screenshot.png`; рабочие артефакты проверок (промежуточные кадры,
битые JSON-фикстуры) — локально в `.verify/` (не коммитится).

## 1. Реально проверено

### Ядро (автоматически, `npm test`: 32/32 за 0.14 с)

| Приёмка ТЗ (§9) | Тесты | Итог |
| --- | --- | --- |
| 1. Связь выкл.: блок 2×2 стабилен; блинкер возвращается через 2 шага; остальные слои пусты | describe «Приёмка 1» | ✅ |
| 2. Межслойное рождение (10,10,1) при связи вкл. / мёртва при выкл. | describe «Приёмка 2» | ✅ |
| 3. Себя не считает; вертикали считаются; диагонали других слоёв нет; граница не зациклена; слои 0↔2 не связаны; максимумы 10/9 | describe «Приёмка 3» (6 тестов) | ✅ |
| 4. Шаг не мутирует вход; результат = синхронный расчёт | describe «Приёмка 4» | ✅ |
| 5. B={0}, S={0}, B={10} применяются; mulberry32 детерминирован (фиксированные значения); JSON round-trip + сохранение поведения после round-trip; 11 вариантов битого JSON отклоняются (версия, width≠32, длина, значения ≠0/1, B/S вне 0..10, speed=31 и т.д.) | describe «Приёмка 5» | ✅ |

### Сборка

- `npm run build` = `tsc --noEmit && vite build` — проходит; бандл 565 КБ
  (предупреждение о >500 КБ — это Three.js, не ошибка).
- Прод-сборка проверена вживую: `vite preview` (порт 4173), сцена рендерится
  (178 461 пикселя не фона), «Шаг» увеличивает поколение, детерминированное
  заполнение seed=42/15 % даёт те же 455 клеток, что и dev-сервер.

### Интерфейс (браузерная автоматизация, dev-сервер; все проверки — числами)

| Требование | Как проверялось | Итог |
| --- | --- | --- |
| «Шаг» = ровно +1 поколение, остаётся на паузе | 2 клика: d1=d2=1, кнопка «Старт» | ✅ |
| Старт/пауза: счёт идёт, на паузе стоит | gen растёт в ране; за 300 мс на паузе +0 | ✅ |
| Горячие клавиши Space / N / 1/2/3 | реальный keydown из puppeteer | ✅ |
| Не перехватывать клавиши в полях ввода | фокус в number-input: Space не запускает | ✅ |
| Добавить/удалить клик-по-клетке; повторный add не дублирует; вне поля — игнор; пауза при правке | реальные mouse-клики: delta +1/0/−1, delta=0 вне поля, кнопка «Старт» | ✅ |
| Контур целевой клетки под курсором; hover не меняет мир | pointermove в режиме add меняет кадр (hover-outline), live 455→455 | ✅ |
| Камера: drag не меняет клетки/поколение **и вращает вид** | liveDelta=0, genDelta=0; пиксели кадра изменились | ✅ |
| Зум колесом доступен в режиме «Добавить», мир не меняется | пиксели изменились, live/gen константа | ✅ |
| Связь вкл/выкл даёт разные исходы + детерминизм | старт→10 шагов: 499 живых; вернуть старт, связь выкл: 270; снова вкл: 499 и **то же распределение по этажам** (158/183/158) | ✅ |
| «Вернуть старт» обнуляет поколение, восстанавливает клетки, сохраняет B/S/связь | старт = 1 клетка → 5 шагов → 0 живых → вернуть → gen 0, live 1; B={2,3}, S={2,3}, связь выкл. сохранились | ✅ |
| Скрытый слой считается в симуляции | 5 шагов со скрытым слоем 1 и без скрытия: идентично (gen 5, live 459, 144/191/124); при этом отрисовка меняется (127 960 → 104 020 светлых пикселей) | ✅ |
| Выбор скрытого слоя делает его видимым | скрыть слой 3, нажать `3`: чекбокс видимости=on, radio=3 | ✅ |
| Раздвижение этажей не меняет расчёт | gap=6, старт→10 шагов: 499 (158/183/158) — совпало с gap=2.5 | ✅ |
| Экспорт: формат и содержимое | перехваченный Blob: formatVersion=1, 32×32×3, generation=3, B{2,3}/S{2,3}/coupling=false, speed=12, seed=777, density=25, cells/start длины 3072 из 0/1 | ✅ |
| Импорт валидного: полное состояние на паузе | после «Очистить» импорт good.json: gen 3, live 1067, speed/seed/density/B/S/связь восстановлены, кнопка «Старт»; «Вернуть старт» → 807 клеток импортированного старта | ✅ |
| Импорт битого: ошибка и мир цел | 4 файла: мусор→«не является корректным JSON»; версия 99→отклонён; cells 3000→отклонён; 1.06 МБ→«превышает 1 МБ»; во всех случаях gen/live **не изменились**, статус с классом error | ✅ |
| Скорость 30 поколений/с выдерживается | ран 4 с: +120 поколений = ровно 30.0 ген/с; rAF ≈ 56.7 fps | ✅ |
| Ошибки консоли | page.on('console'/'pageerror') с начала UI-проверок до конца: **0 записей** | ✅ |
| Визуальная приёмка | скриншоты `.verify/final-full.png`, `final-scene.png` (локально; итоговый ракурс — `docs/screenshot.png`); vision-проверка: 3 этажа, кубики синий/зелёный/оранжевый по легенде, русская панель, чекбоксы 0–10 целиком, без артефактов/ошибочных оверлеев | ✅ |

### Найденные и исправленные в ходе проверок дефекты

1. **Оверлей `#webgl-error` перекрывал canvas, когда скрыт**
   (`display:grid` перебивал `[hidden]`). Добавлено
   `#webgl-error[hidden] { display: none; }`.
2. **Ряды переключателей B/S обрезались**: `repeat(11, 1fr)` не влезал в
   панель 320 px (последний чекбокс на x=1680 при окне 1600). Исправлено на
   перенос `repeat(6, minmax(0,1fr))` (две строки 0–5 / 6–10); overflow
   панели устранён (scrollW=clientW=273), работоспособность переключателей
   перепроверена.

## 2. Не проверено (честно)

- **Safari / Apple Silicon** и другие браузеры — проверялся только headless
  Chromium. Требование ТЗ «не заявлять проверку Safari, если её нет».
- **Производительность на реальном GPU**: 30 ген/с и ~57 fps получены на
  программном SwiftShader — на дискретной GPU будет не хуже, но это не мерялось.
- **Панель при низких окнах**: при высоте 1000 px часть секций («Базовые
  правила», нижняя подсказка о клавишах) уходит за нижний край — панель
  прокручивается (`overflow-y: auto`), поведение корректное, но отдельные
  высоты окон не тестировались.
- **Поля с width ≠ 32**: отклоняются импортом на уровне валидации
  (юнит-тест `width: 16`), но рендер не-32 поля в браузере не проверялся —
  в MVP размер фиксирован.
- **Крупные легальные файлы близко к лимиту (600–900 КБ)** на скорость
  разбора не профилировались; проверенный round-trip-файл (~12 КБ)
  импортируется мгновенно.
- Устоявшаяся **эмержентность** паттернов (глайдеры и т.п.) — не критерий
  приёмки по ТЗ; песочница для её поиска — инструмент, а не проверка.

## 3. Как повторить

```bash
npm test          # ядро
npm run build     # типы + бандл
npm run dev       # ручная проверка UI
```
